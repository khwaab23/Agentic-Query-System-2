# Environment Configuration for Agentic Query System
# IMPORTANT: Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control!

# Required: OpenAI API Key for GPT function calling
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Optional: Customize the OpenAI model (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Optional: Customize API settings (default: 0.1)
# OPENAI_TEMPERATURE=0.1

# SECURITY WARNING:
# Never commit files containing real API keys to version control!
# The .env file is ignored by git for security reasons.
# API_HOST=0.0.0.0
# API_PORT=8000
